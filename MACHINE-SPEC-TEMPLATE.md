# 新機種シミュレーター 仕様テンプレート

新しいパチンコ機種のシミュレーターを作る際、このテンプレートを埋めてClaudeに渡せば、
ブレスト・設計をほぼスキップして実装計画に進める。

**前提（共通フォーマット・毎回そのまま流用するもの）**:
- 払い出し実値/表示値の変換式（後述セクション5）。UIシェルの種類によらず共通。
- 完成後の公開手順（GitHub Pages＋ブログ記事、セクション8）。UIシェルの種類によらず共通。

UIシェル・ファイル構成だけは0で選んだ型に従う。

---

## 0. UIシェルの選択

以下2種類から選ぶ。当てはまらない場合（画面構成が大きく異なる新機種など）は新規UI型として都度設計する。

### A. garo型（3行ヘッダー＋演出ON/OFF設定型）

- 参照実装: [`pachinko-simulator-garo`](https://github.com/wwi195/pachinko-simulator-garo)
- 適したRUSH/LT構造: カタログ①②③（後述）
- ファイル構成: `index.html`/`style.css`/`script.js`/`calc.js`（garoから無変更で流用、UIシェルの共通処理）＋`logic.js`（新規、機種固有の純粋な確率・状態遷移関数、node:testでTDD）
- UI特徴: ヘッダー3行構成（回転数／収支+回転数レート+大当たり総回数／詳細ボタンで展開する内訳パネル）、操作パネル（START・一括回転・演出設定・退店）、演出ON/OFF設定モーダル（片方OFFで信頼度を保ったまま相手に確率が吸収される、両方OFFで告知のみの先祝モード相当）、精算画面、履歴欄、当たり演出の全画面モーダルシステム
- 採用例: `pachinko-simulator-bancho99`（①ST型で採用）

### B. ghoul型（ステータスバー＋インライン画面遷移型）

- 参照実装: [`pachinko-simulator-ghoul`](https://github.com/wwi195/pachinko-simulator-ghoul)
- 適したRUSH/LT構造: カタログ①④（後述）。②③も応用可能
- ファイル構成: `index.html`/`style.css`/`script.js`/`logic.js`（純粋な確率・状態遷移ロジックのみ、DOM非依存、node:testでTDD）。calc.jsに相当する共有UIファイルは無く、スピンレート定数などもlogic.js側に含める
- UI特徴: ヘッダー（持ち球／投資／収支、回転数、大当たり総回数・確率、モードバッジ）、`#main-screen`にscript.js側の`buildScreen(state)`がinnerHTMLで画面テンプレートを差し込む方式（モーダルではなく状態遷移で画面ごと切り替える）、通算成績バー、履歴欄
- 採用例: `pachinko-simulator-lycoris`（白ベース、②相当の隠しモード制LT）、`pachinko-simulator-oumi5sp`（白＋青系、確変ループ＋時短＋遊タイム）、`pachinko-simulator-boukyousei`（黒×赤、④ST+保留型）

## 1. 基本情報

- 機種名:
- 原作/テーマ:
- タイトルバー文言:
- UIシェル: A（garo型）／B（ghoul型）（セクション0参照）

### 配色パターン集（参考。機種イメージに応じて新規配色を作ってもよい）

| パターン | 使用例 | 備考 |
|---|---|---|
| 金/黒基調 | garo・ghoul（デフォルト） | 特に指定がなければこれ |
| 白ベース | lycoris・oumi5sp | 原作が明るい/清潔感のあるイメージのとき |
| 黒×赤基調 | boukyousei | 原作がダーク/バトル系のとき |
| 白＋アクセントカラー | oumi5sp（青系#0d6ea8） | ベースは白、原作のイメージカラー1色をアクセントに差し込む |

- 配色イメージ（上記から選ぶか、原作のイメージに合わせて新規に指定）:

## 2. 遊技条件（ここで決めたら以後固定。プレイヤーによる選択式にはしない）

- 交換レート: （例: 等価＝4円投資/4円換金）
- 回転数レート: プレイヤーが選択式（例: 14/16/18/20回転から選択、デフォルト16）／固定（例: 17回転/千円に固定）
- 軍資金上限: 無し／有り（有りの場合、上限額と閉店処理の仕様も記載）
- 回転数上限（閉店処理）: 無し／有り（有りの場合、閾値と「翌日も続ける」日またぎ機能の要否も記載。日またぎ機能は`pachinko-simulator-boukyousei`の`continueNextDay()`を参照実装とする）

## 3. 通常時の演出構造

予告演出の種類ごとに:

| 演出名 | 出現確率（出現＋ハズレ込み） | 信頼度（出現時の当選率） |
|---|---|---|
| 例: ◯◯保留 | 1/nnn | nn% |
| 例: ◯◯剣 | 1/nnn | nn% |

- 複数の予告演出がある場合、両方独立ON/OFF可能（garo方式）か、排他選択式（`pachinko-simulator-bancho99`方式、実測値ベースで確率が合算からズレる場合はこちら）かを明記
- 小当たり／チャージ的な要素の有無: 有り／無し
  - 有りの場合: 名称・確率・獲得ラウンド数（セクション5参照）・大当たりとの確率分裂の有無（分裂する場合、`pachinko-simulator-boukyousei`のチャージ/図柄揃い分解の考え方を参照）

## 4. RUSH/LT構造タイプ

以下のカタログから選ぶか、当てはまらない場合は新パターンとして定義する。

### カタログ

**① ST型（グールデカ/ghoul型）**
電サポ（ST）回数（例: 130回）が設定され、その間に当たれば回数がリセットされて延長する。
次回当たりの保証はないが、当たり続ける限り理論上無限に連チャンしうる。ST中の当選確率は
通常時とは別レートで抽選される。

**② 確変型（牙狼/garo型）**
大当たり後、次回大当たりがほぼ濃厚な状態（LTモード）に入る。LT中は抽選のたびに
「継続」か「終了」かが決まり、終了に割り振られた確率を引くと必ず通常時に戻る
（＝有限の終了率で必ずいつか終わる、無限ループではない一本道の連チャン構造）。

**③ 1種2種型（キン肉マン型、未実装・要詳細化）**
1種（デジタル抽選）で当たりを引くと、2種（権利物）側の「保留チャージ権利」を獲得する。権利数に応じて後続の大当たり有無・
大小・最低保証が変動する方式。実装時に都度、権利の消費/獲得ルールを具体的に詰める必要がある。

**④ ST+保留型（暴凶星/拳王RUSH型）**
電サポ（ST、例: 10回転）＋残保留（例: 4個）の2段階で構成される「1サイクル」制。サイクル内で
最低1回当たればST・残保留とも初期値にフルリセットして継続、サイクルを全消化（ST+残保留とも
外れ）すると終了する。ST消化中と残保留消化中とで当たり時の振り分けテーブルが異なる場合がある
（例: ST消化中は複数ランクの当たり種別＋出玉なし当選（＝当たり扱いだがサイクルリセットのみで
出玉0、いわゆる「STリセット」）から抽選、残保留消化中はランクが絞られた別テーブルから抽選）。
出玉なし当選の扱い（当たり回数にカウントするか等）を明記すること。

### この機種で採用するタイプ

- タイプ: ①／②／③／④／新規（新規の場合は上記と同じ粒度で構造を記述する）
- 連チャン数の表示方法: 採用したタイプの標準表示に従う（① なら電サポ残り回数、② なら連チャン数カウント、
  ③ なら権利残り数、④ ならST残り／保留残りの2段階表示＋初当たり含む連チャン数、など）

## 5. 払い出し表（実値/表示値は自動導出）

**フォーマット共通の基準値**（`pachinko-simulator-garo`から導出。機種によって実際の基準玉数が
異なる場合はここを上書きする）:

- 1カウントあたりの表示発数: 15発
- 1ラウンドあたりのカウント数: 10カウント
- 1ラウンドあたりの投入コスト（実質減算分）: 10発

→ **1ラウンド = 150発（表示） / 140発（実質）**
　（表示 = ラウンド数 × 150、実質 = ラウンド数 × 140）

機種固有で入力するのは **ラウンド数だけ**でよい:

| ボーナス名称 | ラウンド数 | 表示発数（自動算出） | 実質発数（自動算出） |
|---|---|---|---|
| 例: 1500ボーナス | 10 | 1500 | 1400 |
| 例: 7500ボーナス | 50 | 7500 | 7000 |
| 例: チャージ | 2 | 300 | 280 |
| （機種固有のボーナスを追記） | | | |

上記の表示/実質＝14/15比率は、`pachinko-simulator-ghoul`／`boukyousei`など（UIシェルB）でも
同じ換算率で踏襲している。UIシェルBでは`logic.js`側に各当たり種別の`balls`（表示）と`actual`
（実質）を直接埋める形で実装する（`ZUGAR_TYPES`/`RUSH_HIT_TYPES`を参照）。表示は今まで通り
`balls`を出し、収支計算（持ち球加算）には`actual`を使う。

## 6. 演出ON/OFF設定の対象（UIシェルAのみ）

`pachinko-simulator-garo`の「演出設定」機能をそのまま使うかどうか。UIシェルB採用時はこの
セクションは対象外（演出は状態遷移の一部として素朴に表示する）。

- 対象にする演出: （セクション3の予告演出のうち、ON/OFF切り替え対象にするもの。通常は全部）
- 両方OFF時のモード名・告知文言: （例: 「先祝モード」「🎉 先祝告知発生！」。機種テーマに合わせて変更可）

## 7. ヘッダー詳細パネルの内訳項目

「詳細を見る」を押した時に表示する項目（持ち球・投資・図柄揃い合計は共通で常に含む。
それ以外に機種固有で追加する内訳があれば記載）:

- （例: 極限突入／非突入、チャージ回数、極限バトル成功／失敗、LT内訳など）
- 通常時／RUSH中それぞれの当たりに、当選確率・振り分け割合（実機公表値ベース）をその場で
  表示するかどうか（`pachinko-simulator-boukyousei`の「当選確率 1/399.8のN%」「振り分け N%」
  表示を参照実装とする。実機公表値の割合と、実装上正規化した抽選ウェイトが異なる場合は
  `displayPercent`のような表示専用フィールドを別途持たせる）

## 8. 完成後の公開（実装完了後、毎回行う）

1. GitHub上に同アカウント（`wwi195`）でPublicリポジトリを作成しpush（`gh repo create <owner>/<name> --public --source=. --remote=origin`）
2. GitHub Pagesを有効化（`gh api -X POST repos/<owner>/<name>/pages -f build_type=legacy -f source[branch]=master -f source[path]=/`）。デプロイ完了まで数十秒〜1分程度かかるため、`curl -sI`で200が返るまで待ってから次に進む
3. `blog.md`をプロジェクトルートに作成し、以下を含める（`pachinko-simulator-boukyousei/blog.md`を参照実装とする）:
   - きっかけ（なぜこの機種のシミュレーターを作ったか）
   - シミュレーターの紹介（できること）と使い方（GitHub PagesのURLを明記）
   - 収支のボーダーライン（千円あたり回転数）の理論値。機種の確率モデルから実際に計算して算出する（RUSH/LTが再生過程になっている場合は期待値の再生方程式で導出。詳細な導出コードは`pachinko-simulator-boukyousei`のセッション履歴を参照）
4. `blog.md`をコミットしてpush

---

## 使い方

1. このテンプレートを埋める（画像素材の登録は不要。画像は実装時に別途用意する）
2. 埋めた内容をClaudeに渡し、セクション0で選んだUIシェルの参照実装をベースに新規プロジェクトを立ち上げる
3. 通常の brainstorming → 設計書 → 実装計画 → 実装 の流れに乗せる（このテンプレートが設計書の大部分を代替する）
4. 実装完了後、セクション8の公開手順を行う
